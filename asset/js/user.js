(function (win, $, doc, undefined) {
    'use strict';

    const user = (function(){

        const _configs = {

        };

        const init = () => {
            console.log('[USER]', 'init');
        };
    });

}(window, jQuery, document));
